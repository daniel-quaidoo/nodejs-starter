export enum ViewingType {
    IN_PERSON = 'in_person',
    VIRTUAL = 'virtual',
}

export enum ViewingStatus {
    INCOMING = 'incoming',
    COMPLETED = 'completed',
    CANCELLED = 'cancelled',
}

export enum ReservationStatus {
    PENDING = 'pending',
    CONFIRMED = 'confirmed',
    CANCELLED = 'cancelled',
    EXPIRED = 'expired',
}
